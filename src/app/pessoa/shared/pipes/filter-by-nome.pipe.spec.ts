import { FilterByNomePipe } from './filter-by-nome.pipe';

describe('FilterByNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNomePipe();
    expect(pipe).toBeTruthy();
  });
});
